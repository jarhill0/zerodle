name: Push Site
on:
  push:
    branches: [main]
jobs:
  Push-Site:
    runs-on: jarhill0/rclone
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: whoami
      - run: rclone --version
      - run: ./ci-upload.sh
